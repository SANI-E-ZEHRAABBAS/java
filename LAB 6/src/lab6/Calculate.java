package lab6;

public class Calculate {

	double findsquareroot(double num) {
		return Math.sqrt(num);
	}
	double calcpower(double base, double exponent){
		return Math.pow(base, exponent);
	}
	int findDifference(int a, int b) {
        return Math.abs(a - b);
    }
}
